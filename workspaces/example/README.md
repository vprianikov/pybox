# Example

Example workspace for trying code snippets and notebooks inside `pybox`.

## What is here

- `example.ipynb` - starter notebook for experiments.
- `pyproject.toml` - workspace metadata and local dependencies.

## Run

From the repository root:

```bash
uv sync --package example
```

Then open `workspaces/example/example.ipynb`.
